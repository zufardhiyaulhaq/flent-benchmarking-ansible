- name: sleep
  pause:
    seconds: 10

- name: run script
  shell: bash /root/flent/generate_graphic.sh
  environment:
    type: "{{ type }}"
    scenario: "{{ scenario }}"
    ovnovs: "{{ ovnovs }}"